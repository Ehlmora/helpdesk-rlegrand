<nav class="col-md-3 col-lg-2 d-md-block sidebar shadow">
    <div class="sidebar-sticky pt-3">
    <a class="navbar-brand col-md-3 col-lg-2 mr-0 px-3" href="
        {% if is_granted('ROLE_ADMIN') %}
            {{ path('admin.ticket.index') }}
        {% elseif is_granted('ROLE_CLIENT') %}
            {{ path('client.ticket.index') }}
        {% else %}
            {{ path('login') }}
        {% endif %}
    ">D&R Informatique</a>
    <hr class="sidebar-divider">
    {% if is_granted('ROLE_ADMIN') %}
        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 ">Tickets</h6>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'admin.ticket.index' %} active {% endif %}" href="{{ path('admin.ticket.index') }}">Mes tickets</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'admin.ticket.new' %} active {% endif %}" href="{{ path('admin.ticket.new') }}">Nouveau ticket</a>
            </li>
        </ul>
        <hr class="sidebar-divider">
        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 ">Utilisateurs</h6>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'admin.user.index' %} active {% endif %}" href="{{ path('admin.user.index') }}">Utilisateurs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'admin.user.new' %} active {% endif %}" href="{{ path('admin.user.new') }}">Nouvel utilisateur</a>
            </li>
        </ul>
    
    {% elseif is_granted('ROLE_CLIENT') %}
        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 ">Tickets</h6>
        <ul class="nav flex-column">
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'client.ticket.index' %} active {% endif %}" href="{{ path('client.ticket.index') }}">Mes tickets</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if current_menu is defined and current_menu == 'client.ticket.new' %} active {% endif %}" href="{{ path('client.ticket.new') }}">Nouveau ticket</a>
            </li>
        </ul>
    {% else %}

    {% endif %}
    </div>
</nav>
